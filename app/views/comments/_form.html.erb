<%= form_with(model: comment, local: true) do |form| %>
  <% if comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :post_id %>
    <%= form.number_field :post_id, id: :comment_post_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :comment_user_id %>
  </div>

  <div class="field">
    <%= form.label :comments %>
    <%= form.text_field :comments, id: :comment_comments %>
  </div>

  <div class="field">
    <%= form.label :like %>
    <%= form.check_box :like, id: :comment_like %>
  </div>

  <div class="field">
    <%= form.label :modified_date %>
    <%= form.datetime_select :modified_date, id: :comment_modified_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
